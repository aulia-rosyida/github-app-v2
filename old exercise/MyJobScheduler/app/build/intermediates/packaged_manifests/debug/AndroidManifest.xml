<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.auliarosyida.myjobscheduler"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="30" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.MyJobScheduler" >
        <activity android:name="com.dicoding.auliarosyida.myjobscheduler.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
            daftarkan kelas JobService agar dikenali oleh Android System.
            Bila belum didaftarkan, maka job tersebut tidak akan dijalankan oleh Android.
            Konsekuensi lain adalah kegagalan di sistem, karena job tersebut tidak dapat ditemukan.
        -->
        <service
            android:name="com.dicoding.auliarosyida.myjobscheduler.GetCurrentWeatherJobService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>